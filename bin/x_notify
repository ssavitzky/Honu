#!/bin/csh -f
#
#	Notify the user about today's events, if any.
#
set dt = (`date`)
@ dd = $dt[3] + 0
set dm = xc$dd$dt[2]$dt[6]
set cf = ~/Calendar/$dm

if (-e $cf) then
   if (-e /usr/bin/X11/xstring) then
      /usr/bin/X11/xstring -g -0+222 "`head -1 $cf`"&
   endif
endif
